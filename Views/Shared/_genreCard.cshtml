@model Genre
<div class="col-lg-3">
    <div class="categories__item set-bg" data-setbg=@Model.BookList.First().PictureName>
        <h5><a asp-controller="Books" asp-action="Index">@Model.GenreName</a></h5>
    </div>
</div>
